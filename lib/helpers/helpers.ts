/**
 * Generic, framework- and page-agnostic utilities — e.g. array sorting, string
 * cleanup, date formatting. Exposed via the `helpers` fixture:
 *
 *   test("…", async ({ helpers }) => { helpers.sortBy(...) });
 *
 * Page flows live on the page objects; test data lives in lib/data.
 * (Empty for now — add utilities here as they are needed.)
 */
export class Helpers {}
